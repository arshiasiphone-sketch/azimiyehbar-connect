# راهنمای استقرار پروژه کرج باربری در لیارا

## 📋 پیش‌نیازها

- حساب کاربری در [لیارا](https://liara.ir)
- [Liara CLI](https://docs.liara.ir/cli/install) نصب شده
- Node.js نسخه 18 یا بالاتر

## 🚀 مراحل استقرار

### ۱. آماده‌سازی پروژه

```bash
# Clone یا دانلود پروژه از GitHub
git clone <your-repo-url>
cd karaj-barbari

# نصب وابستگی‌ها
npm install

# ساخت نسخه production
npm run build
```

### ۲. ایجاد پروژه در لیارا

```bash
# ورود به لیارا
liara login

# ایجاد پروژه استاتیک
liara deploy --platform=static
```

### ۳. پیکربندی متغیرهای محیطی

در داشبورد لیارا، این متغیرها را اضافه کنید:

```env
VITE_SUPABASE_URL=https://urbabtkeofqgljgmxwrq.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## 📁 ساختار فایل‌های Build

پس از اجرای `npm run build`، پوشه `dist` ایجاد می‌شود:

```
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── ...
├── robots.txt
└── sitemap.xml
```

## 🔧 فایل پیکربندی لیارا

فایل `liara.json` در ریشه پروژه ایجاد کنید:

```json
{
  "platform": "static",
  "static": {
    "path": "dist",
    "spa": true
  }
}
```

## 🌐 تنظیم دامنه

### دامنه رایگان لیارا
پس از deploy، آدرسی مانند `your-app.liara.run` دریافت می‌کنید.

### دامنه اختصاصی
1. در داشبورد لیارا به بخش "دامنه‌ها" بروید
2. دامنه خود را اضافه کنید
3. رکوردهای DNS را تنظیم کنید:
   - CNAME: `@` → `your-app.liara.run`
   - یا A record به IP لیارا

## 📊 دیتابیس

### گزینه ۱: استفاده از Supabase (توصیه شده)
پروژه در حال حاضر به Supabase متصل است و نیازی به تغییر ندارد.

### گزینه ۲: PostgreSQL لیارا
1. یک دیتابیس PostgreSQL در لیارا ایجاد کنید
2. فایل `docs/database/schema.sql` را اجرا کنید
3. فایل `docs/database/seed.sql` را برای داده‌های اولیه اجرا کنید
4. متغیرهای محیطی را به‌روز کنید

## 🔐 Edge Functions

Edge Functions فقط در Supabase اجرا می‌شوند. اگر از دیتابیس لیارا استفاده می‌کنید:

### جایگزین SMS.ir
یک سرویس backend جدا نیاز دارید:
- Node.js API در لیارا
- یا Cloud Functions دیگر

## ✅ چک‌لیست قبل از Production

- [ ] متغیرهای محیطی تنظیم شده
- [ ] دامنه و SSL فعال
- [ ] تست فرم‌های ارسال
- [ ] تست پنل ادمین
- [ ] بررسی SEO با Google Search Console
- [ ] تست سرعت با PageSpeed Insights

## 🆘 عیب‌یابی

### خطای 404 در صفحات
- مطمئن شوید `spa: true` در `liara.json` تنظیم شده

### مشکل اتصال به دیتابیس
- متغیرهای محیطی را بررسی کنید
- CORS در Supabase را چک کنید

### عدم ارسال SMS
- Edge Function در Supabase deploy شده باشد
- کلیدهای SMS.ir صحیح باشند

## 📞 پشتیبانی

- [مستندات لیارا](https://docs.liara.ir)
- [مستندات Supabase](https://supabase.com/docs)
